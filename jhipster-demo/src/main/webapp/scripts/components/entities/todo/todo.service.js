'use strict';

angular.module('ucllApp')
    .factory('Todo', function ($resource, DateUtils) {
        return $resource('api/todos/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
